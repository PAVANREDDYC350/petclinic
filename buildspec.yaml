version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
    commands:
      - echo "Installing dependencies"
      - mvn -version

  build:
    commands:
      - echo "Running Maven build"
      - mvn clean test

artifacts:
  files:
    - target/*.jar